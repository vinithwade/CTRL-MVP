{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "installCommand": "npm install --workspaces",
  "buildCommand": "npm run build --workspace=shared --if-present && npm run build --workspace=frontend",
  "outputDirectory": "packages/frontend/dist"
} 