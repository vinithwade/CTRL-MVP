{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "installCommand": "npm install && npm install --prefix packages/shared && npm install --prefix packages/frontend",
  "buildCommand": "npm run build --prefix packages/shared && npm run build --prefix packages/frontend",
  "outputDirectory": "packages/frontend/dist"
} 