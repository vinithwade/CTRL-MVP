{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "installCommand": "npm install && cd packages/shared && npm install && cd .. && cd frontend && npm install && cd ../..",
  "buildCommand": "cd packages/shared && npm run build && cd .. && cd frontend && npm run build && cd ../..",
  "outputDirectory": "packages/frontend/dist"
} 