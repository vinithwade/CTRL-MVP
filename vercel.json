{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "installCommand": "npm install && (cd packages/shared && npm install) && (cd packages/frontend && npm install)",
  "buildCommand": "(cd packages/shared && npm run build) && (cd packages/frontend && npm run build)",
  "outputDirectory": "packages/frontend/dist"
} 